{
  "name": "html-to-pdf-service",
  "version": "1.0.0",
  "description": "Express service with isolated and optimized endpoints to convert HTML to PDF using Puppeteer",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
  "start": "node index.js",
  "dev": "nodemon index.js",
  "postinstall": "puppeteer install chrome",
  "render-build": "npm ci && npm run postinstall"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
  "puppeteer": "^23.7.1",
  "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
